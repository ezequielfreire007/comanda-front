import { OrangeDirective } from './orange.directive';

describe('OrangeDirective', () => {
  it('should create an instance', () => {
    const directive = new OrangeDirective();
    expect(directive).toBeTruthy();
  });
});
