import { ValidarRolesDirective } from './validar-roles.directive';

describe('ValidarRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarRolesDirective();
    expect(directive).toBeTruthy();
  });
});
