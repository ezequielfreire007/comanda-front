import { EstadosMesaPipe } from './estados-mesa.pipe';

describe('EstadosMesaPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadosMesaPipe();
    expect(pipe).toBeTruthy();
  });
});
