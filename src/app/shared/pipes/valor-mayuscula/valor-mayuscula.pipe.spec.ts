import { ValorMayusculaPipe } from './valor-mayuscula.pipe';

describe('ValorMayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new ValorMayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
